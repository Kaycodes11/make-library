// if other component's default is like like : export { default } from "./Button"; then below
// export { default as Button } from "./Button";

// if other component's default is like like : export * from "./Button"; then below

export * from "./Button";
export * from "./Input";



